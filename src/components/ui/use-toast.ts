
// Re-export from the hooks folder
import { useToast, toast, ToastProvider } from "@/hooks/use-toast";
import { ToastType } from "@/hooks/use-toast";

export { useToast, toast, ToastProvider, type ToastType };
